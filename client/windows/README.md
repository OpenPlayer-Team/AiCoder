# NovaCode Cloud Windows Client

This client is intentionally lightweight. It does not run the LLM locally.

Requirements:
- Windows 10/11
- Git for Windows
- PowerShell 5.1+
- network access to GitHub/Kaggle

The GPU/model/OpenCode backend runs in the Kaggle runtime. Keep source code synchronized through GitHub rather than copying model weights to Windows.
