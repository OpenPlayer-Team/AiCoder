# NovaCode Cloud runtime compatibility constraints.
# Kaggle's preinstalled CUDA/PyTorch stack is authoritative; install_dependencies.sh
# does NOT blindly install this file. These are minimum Python package families
# expected by the scripts when the corresponding packages are absent.
transformers>=4.45
huggingface-hub>=0.25
accelerate>=0.34
openai>=1.50
requests>=2.32
